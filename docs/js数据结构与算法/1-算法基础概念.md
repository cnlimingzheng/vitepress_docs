# 算法基础概念 ⭐⭐⭐⭐⭐

##  什么是算法？

**算法**就像是解决问题的"食谱"或"说明书"。就像做菜需要按照菜谱的步骤一样，计算机解决问题也需要按照算法的步骤来执行。

###  算法的定义
算法是解决特定问题的有限步骤的集合，具有以下特征：
- **输入**：有零个或多个输入
- **输出**：有一个或多个输出
- **有穷性**：步骤数量是有限的
- **确定性**：每个步骤都有明确的含义
- **可行性**：每个步骤都能在有限时间内完成

###  生活中的算法例子

**例子1：泡方便面的算法**
```
1. 烧开水
2. 撕开方便面包装
3. 将面饼放入碗中
4. 倒入调料包
5. 倒入开水
6. 等待3分钟
7. 搅拌均匀
8. 开始享用
```

**例子2：找到数组中最大值的算法**
```javascript
/**
 * 找到数组中的最大值
 * @param {number[]} arr - 输入的数字数组
 * @returns {number} 数组中的最大值
 */
function findMax(arr) {
    // 边界条件检查：如果数组为空，返回undefined
    if (arr.length === 0) {
        return undefined;
    }
    
    // 假设第一个元素是最大值
    let max = arr[0];
    
    // 从第二个元素开始遍历
    for (let i = 1; i < arr.length; i++) {
        // 如果当前元素比max大，更新max
        if (arr[i] > max) {
            max = arr[i];
        }
    }
    
    return max;
}

// 测试代码
const numbers = [3, 7, 2, 9, 1, 5];
const maxValue = findMax(numbers);
console.log(`数组 [${numbers}] 中的最大值是: ${maxValue}`); // 输出: 9
```

### Python实现

```python
def find_max(arr):
    """
    找到数组中的最大值
    
    Args:
        arr (list): 输入的数字数组
        
    Returns:
        int/float: 数组中的最大值，如果数组为空返回None
    """
    # 边界条件检查：如果数组为空，返回None
    if len(arr) == 0:
        return None
    
    # 假设第一个元素是最大值
    max_val = arr[0]
    
    # 从第二个元素开始遍历
    for i in range(1, len(arr)):
        # 如果当前元素比max_val大，更新max_val
        if arr[i] > max_val:
            max_val = arr[i]
    
    return max_val

# 测试代码
numbers = [3, 7, 2, 9, 1, 5]
max_value = find_max(numbers)
print(f"数组 {numbers} 中的最大值是: {max_value}")  # 输出: 9
```

### Java实现

```java
public class ArrayOperations {
    /**
     * 找到数组中的最大值
     * 
     * @param arr 输入的数字数组
     * @return 数组中的最大值
     * @throws IllegalArgumentException 当数组为空时抛出异常
     */
    public static Integer findMax(int[] arr) {
        // 边界条件检查：如果数组为空，返回null
        if (arr.length == 0) {
            return null;
        }
        
        // 假设第一个元素是最大值
        int max = arr[0];
        
        // 从第二个元素开始遍历
        for (int i = 1; i < arr.length; i++) {
            // 如果当前元素比max大，更新max
            if (arr[i] > max) {
                max = arr[i];
            }
        }
        
        return max;
    }
    
    // 测试代码
    public static void main(String[] args) {
        int[] numbers = {3, 7, 2, 9, 1, 5};
        Integer maxValue = findMax(numbers);
        System.out.println("数组中的最大值是: " + maxValue); // 输出: 9
    }
}
```

##  算法的重要性

### 为什么要学习算法？

1. **提升编程能力**：算法是编程的核心，掌握算法能让你写出更高效的代码
2. **解决复杂问题**：面对复杂问题时，算法思维能帮你分解问题、找到解决方案
3. **面试必备**：几乎所有技术面试都会考察算法能力
4. **职业发展**：算法能力是高级工程师的必备技能

###  算法思维的培养

**分治思想**：把大问题分解成小问题
```javascript
/**
 * 计算数组元素的总和（分治思想）
 * @param {number[]} arr - 数字数组
 * @param {number} start - 起始索引
 * @param {number} end - 结束索引
 * @returns {number} 数组元素的总和
 */
function sumArray(arr, start = 0, end = arr.length - 1) {
    // 基础情况：只有一个元素
    if (start === end) {
        return arr[start];
    }
    
    // 基础情况：没有元素
    if (start > end) {
        return 0;
    }
    
    // 分治：将数组分成两半
    const mid = Math.floor((start + end) / 2);
    const leftSum = sumArray(arr, start, mid);     // 左半部分的和
    const rightSum = sumArray(arr, mid + 1, end);  // 右半部分的和
    
    return leftSum + rightSum; // 合并结果
}

// 测试代码
const testArray = [1, 2, 3, 4, 5];
const total = sumArray(testArray);
console.log(`数组 [${testArray}] 的总和是: ${total}`); // 输出: 15
```

### Python实现

```python
def sum_array(arr, start=None, end=None):
    """
    计算数组元素的总和（分治思想）
    
    Args:
        arr (list): 数字数组
        start (int): 起始索引，默认为0
        end (int): 结束索引，默认为数组长度-1
        
    Returns:
        int/float: 数组元素的总和
    """
    # 设置默认值
    if start is None:
        start = 0
    if end is None:
        end = len(arr) - 1
    
    # 基础情况：只有一个元素
    if start == end:
        return arr[start]
    
    # 基础情况：没有元素
    if start > end:
        return 0
    
    # 分治：将数组分成两半
    mid = (start + end) // 2
    left_sum = sum_array(arr, start, mid)      # 左半部分的和
    right_sum = sum_array(arr, mid + 1, end)   # 右半部分的和
    
    return left_sum + right_sum  # 合并结果

# 测试代码
test_array = [1, 2, 3, 4, 5]
total = sum_array(test_array)
print(f"数组 {test_array} 的总和是: {total}")  # 输出: 15
```

### Java实现

```java
public class DivideAndConquer {
    /**
     * 计算数组元素的总和（分治思想）
     * 
     * @param arr 数字数组
     * @param start 起始索引
     * @param end 结束索引
     * @return 数组元素的总和
     */
    public static long sumArray(int[] arr, int start, int end) {
        // 基础情况：只有一个元素
        if (start == end) {
            return arr[start];
        }
        
        // 基础情况：没有元素
        if (start > end) {
            return 0;
        }
        
        // 分治：将数组分成两半，避免整数溢出
        int mid = start + (end - start) / 2;
        long leftSum = sumArray(arr, start, mid);      // 左半部分的和
        long rightSum = sumArray(arr, mid + 1, end);   // 右半部分的和
        
        return leftSum + rightSum; // 合并结果
    }
    
    /**
     * 重载方法：使用默认的开始和结束索引
     */
    public static long sumArray(int[] arr) {
        if (arr.length == 0) {
            return 0;
        }
        return sumArray(arr, 0, arr.length - 1);
    }
    
    // 测试代码
    public static void main(String[] args) {
        int[] testArray = {1, 2, 3, 4, 5};
        long total = sumArray(testArray);
        System.out.println("数组的总和是: " + total); // 输出: 15
    }
}
```

##  算法设计的基本方法

### 1. 暴力法（Brute Force）
最直接的方法，尝试所有可能的解决方案

```javascript
/**
 * 在数组中查找两个数，使它们的和等于目标值（暴力法）
 * @param {number[]} nums - 数字数组
 * @param {number} target - 目标和
 * @returns {number[]} 两个数的索引，如果不存在返回空数组
 */
function twoSumBruteForce(nums, target) {
    // 外层循环：选择第一个数
    for (let i = 0; i < nums.length - 1; i++) {
        // 内层循环：选择第二个数
        for (let j = i + 1; j < nums.length; j++) {
            // 检查两数之和是否等于目标值
            if (nums[i] + nums[j] === target) {
                return [i, j]; // 返回索引
            }
        }
    }
    return []; // 没找到符合条件的数对
}

// 测试代码
const nums = [2, 7, 11, 15];
const target = 9;
const result = twoSumBruteForce(nums, target);
console.log(`在数组 [${nums}] 中找到和为 ${target} 的两个数的索引: [${result}]`);
// 输出: [0, 1] (因为 nums[0] + nums[1] = 2 + 7 = 9)
```

### Python实现

```python
def two_sum_brute_force(nums, target):
    """
    在数组中查找两个数，使它们的和等于目标值（暴力法）
    
    Args:
        nums (list): 数字数组
        target (int): 目标和
        
    Returns:
        list: 两个数的索引，如果不存在返回空列表
    """
    # 外层循环：选择第一个数
    for i in range(len(nums) - 1):
        # 内层循环：选择第二个数
        for j in range(i + 1, len(nums)):
            # 检查两数之和是否等于目标值
            if nums[i] + nums[j] == target:
                return [i, j]  # 返回索引
    return []  # 没找到符合条件的数对

# 测试代码
nums = [2, 7, 11, 15]
target = 9
result = two_sum_brute_force(nums, target)
print(f"在数组 {nums} 中找到和为 {target} 的两个数的索引: {result}")
# 输出: [0, 1] (因为 nums[0] + nums[1] = 2 + 7 = 9)
```

### Java实现

```java
import java.util.Arrays;

public class TwoSumBruteForce {
    /**
     * 在数组中查找两个数，使它们的和等于目标值（暴力法）
     * 
     * @param nums 数字数组
     * @param target 目标和
     * @return 两个数的索引，如果不存在返回空数组
     */
    public static int[] twoSumBruteForce(int[] nums, int target) {
        // 外层循环：选择第一个数
        for (int i = 0; i < nums.length - 1; i++) {
            // 内层循环：选择第二个数
            for (int j = i + 1; j < nums.length; j++) {
                // 检查两数之和是否等于目标值
                if (nums[i] + nums[j] == target) {
                    return new int[]{i, j}; // 返回索引
                }
            }
        }
        return new int[]{}; // 没找到符合条件的数对
    }
    
    // 测试代码
    public static void main(String[] args) {
        int[] nums = {2, 7, 11, 15};
        int target = 9;
        int[] result = twoSumBruteForce(nums, target);
        System.out.println("在数组 " + Arrays.toString(nums) + " 中找到和为 " + target + " 的两个数的索引: " + Arrays.toString(result));
        // 输出: [0, 1] (因为 nums[0] + nums[1] = 2 + 7 = 9)
    }
}
```

### 2. 贪心法（Greedy）
每一步都选择当前最优的解决方案

```javascript
/**
 * 找零钱问题（贪心算法）
 * 假设有无限数量的硬币：100元、50元、20元、10元、5元、1元
 * @param {number} amount - 需要找零的金额
 * @returns {Object} 每种面额硬币的数量
 */
function makeChange(amount) {
    // 硬币面额（从大到小排序）
    const coins = [100, 50, 20, 10, 5, 1];
    const result = {};
    
    // 贪心策略：优先使用面额最大的硬币
    for (let coin of coins) {
        if (amount >= coin) {
            const count = Math.floor(amount / coin); // 计算需要多少个这种硬币
            result[coin] = count;
            amount -= coin * count; // 更新剩余金额
        }
    }
    
    return result;
}

// 测试代码
const changeAmount = 287;
const change = makeChange(changeAmount);
console.log(`找零 ${changeAmount} 元需要的硬币:`);
for (let [coin, count] of Object.entries(change)) {
    console.log(`${coin}元硬币: ${count}个`);
}
// 输出:
// 100元硬币: 2个
// 50元硬币: 1个
// 20元硬币: 1个
// 10元硬币: 1个
// 5元硬币: 1个
// 1元硬币: 2个
```

### Python实现

```python
def make_change(amount):
    """
    找零钱问题（贪心算法）
    假设有无限数量的硬币：100元、50元、20元、10元、5元、1元
    
    Args:
        amount (int): 需要找零的金额
        
    Returns:
        dict: 每种面额硬币的数量
    """
    # 硬币面额（从大到小排序）
    coins = [100, 50, 20, 10, 5, 1]
    result = {}
    
    # 贪心策略：优先使用面额最大的硬币
    for coin in coins:
        if amount >= coin:
            count = amount // coin  # 计算需要多少个这种硬币
            result[coin] = count
            amount -= coin * count  # 更新剩余金额
    
    return result

# 测试代码
change_amount = 287
change = make_change(change_amount)
print(f"找零 {change_amount} 元需要的硬币:")
for coin, count in change.items():
    print(f"{coin}元硬币: {count}个")
# 输出:
# 100元硬币: 2个
# 50元硬币: 1个
# 20元硬币: 1个
# 10元硬币: 1个
# 5元硬币: 1个
# 1元硬币: 2个
```

### Java实现

```java
import java.util.*;

public class CoinChange {
    /**
     * 找零钱问题（贪心算法）
     * 假设有无限数量的硬币：100元、50元、20元、10元、5元、1元
     * 
     * @param amount 需要找零的金额
     * @return 每种面额硬币的数量
     */
    public static Map<Integer, Integer> makeChange(int amount) {
        // 硬币面额（从大到小排序）
        int[] coins = {100, 50, 20, 10, 5, 1};
        Map<Integer, Integer> result = new LinkedHashMap<>();
        
        // 贪心策略：优先使用面额最大的硬币
        for (int coin : coins) {
            if (amount >= coin) {
                int count = amount / coin; // 计算需要多少个这种硬币
                result.put(coin, count);
                amount -= coin * count; // 更新剩余金额
            }
        }
        
        return result;
    }
    
    // 测试代码
    public static void main(String[] args) {
        int changeAmount = 287;
        Map<Integer, Integer> change = makeChange(changeAmount);
        System.out.println("找零 " + changeAmount + " 元需要的硬币:");
        for (Map.Entry<Integer, Integer> entry : change.entrySet()) {
            System.out.println(entry.getKey() + "元硬币: " + entry.getValue() + "个");
        }
        // 输出:
        // 100元硬币: 2个
        // 50元硬币: 1个
        // 20元硬币: 1个
        // 10元硬币: 1个
        // 5元硬币: 1个
        // 1元硬币: 2个
    }
}
```

##  算法学习的方法

### 1. 理解问题
- 仔细阅读问题描述
- 明确输入和输出
- 找出约束条件

### 2. 设计算法
- 先用自然语言描述解决步骤
- 画图帮助理解
- 考虑边界情况

### 3. 编写代码
- 选择合适的数据结构
- 实现算法逻辑
- 添加必要的注释

### 4. 测试验证
- 准备测试用例
- 包括正常情况和边界情况
- 验证结果的正确性

##  实践练习

### 练习1：判断一个数是否为质数
```javascript
/**
 * 判断一个数是否为质数
 * 质数：只能被1和自己整除的大于1的自然数
 * @param {number} n - 要判断的数
 * @returns {boolean} 是质数返回true，否则返回false
 */
function isPrime(n) {
    // 小于2的数不是质数
    if (n < 2) {
        return false;
    }
    
    // 2是质数
    if (n === 2) {
        return true;
    }
    
    // 偶数（除了2）不是质数
    if (n % 2 === 0) {
        return false;
    }
    
    // 只需要检查到√n即可
    // 因为如果n有大于√n的因子，那么必然有小于√n的因子
    const sqrtN = Math.sqrt(n);
    for (let i = 3; i <= sqrtN; i += 2) {
        if (n % i === 0) {
            return false; // 找到因子，不是质数
        }
    }
    
    return true; // 没找到因子，是质数
}

// 测试代码
const testNumbers = [2, 3, 4, 5, 17, 25, 29];
testNumbers.forEach(num => {
    console.log(`${num} ${isPrime(num) ? '是' : '不是'}质数`);
});
```

### Python实现

```python
import math

def is_prime(n):
    """
    判断一个数是否为质数
    质数：只能被1和自己整除的大于1的自然数
    
    Args:
        n (int): 要判断的数
        
    Returns:
        bool: 是质数返回True，否则返回False
    """
    # 小于2的数不是质数
    if n < 2:
        return False
    
    # 2是质数
    if n == 2:
        return True
    
    # 偶数（除了2）不是质数
    if n % 2 == 0:
        return False
    
    # 只需要检查到√n即可
    # 因为如果n有大于√n的因子，那么必然有小于√n的因子
    sqrt_n = int(math.sqrt(n)) + 1
    for i in range(3, sqrt_n, 2):
        if n % i == 0:
            return False  # 找到因子，不是质数
    
    return True  # 没找到因子，是质数

# 测试代码
test_numbers = [2, 3, 4, 5, 17, 25, 29]
for num in test_numbers:
    result = "是" if is_prime(num) else "不是"
    print(f"{num} {result}质数")
```

### Java实现

```java
public class PrimeChecker {
    /**
     * 判断一个数是否为质数
     * 质数：只能被1和自己整除的大于1的自然数
     * 
     * @param n 要判断的数
     * @return 是质数返回true，否则返回false
     */
    public static boolean isPrime(int n) {
        // 小于2的数不是质数
        if (n < 2) {
            return false;
        }
        
        // 2是质数
        if (n == 2) {
            return true;
        }
        
        // 偶数（除了2）不是质数
        if (n % 2 == 0) {
            return false;
        }
        
        // 只需要检查到√n即可
        // 因为如果n有大于√n的因子，那么必然有小于√n的因子
        int sqrtN = (int) Math.sqrt(n);
        for (int i = 3; i <= sqrtN; i += 2) {
            if (n % i == 0) {
                return false; // 找到因子，不是质数
            }
        }
        
        return true; // 没找到因子，是质数
    }
    
    // 测试代码
    public static void main(String[] args) {
        int[] testNumbers = {2, 3, 4, 5, 17, 25, 29};
        for (int num : testNumbers) {
            String result = isPrime(num) ? "是" : "不是";
            System.out.println(num + " " + result + "质数");
        }
    }
}
```

### 练习2：反转字符串
```javascript
/**
 * 反转字符串的多种方法
 */

// 方法1：使用内置方法
function reverseString1(str) {
    return str.split('').reverse().join('');
}

// 方法2：使用循环
function reverseString2(str) {
    let result = '';
    for (let i = str.length - 1; i >= 0; i--) {
        result += str[i];
    }
    return result;
}

// 方法3：使用递归
function reverseString3(str) {
    // 基础情况：空字符串或单个字符
    if (str.length <= 1) {
        return str;
    }
    
    // 递归：第一个字符放到最后，其余字符递归反转
    return reverseString3(str.slice(1)) + str[0];
}

// 测试代码
const testString = "Hello World";
console.log(`原字符串: ${testString}`);
console.log(`方法1反转: ${reverseString1(testString)}`);
console.log(`方法2反转: ${reverseString2(testString)}`);
console.log(`方法3反转: ${reverseString3(testString)}`);
```

### Python实现

```python
def reverse_string_builtin(s):
    """
    方法1：使用Python内置的切片功能
    
    Args:
        s (str): 输入字符串
        
    Returns:
        str: 反转后的字符串
    """
    return s[::-1]

def reverse_string_loop(s):
    """
    方法2：使用循环
    
    Args:
        s (str): 输入字符串
        
    Returns:
        str: 反转后的字符串
    """
    result = ''
    for i in range(len(s) - 1, -1, -1):
        result += s[i]
    return result

def reverse_string_recursive(s):
    """
    方法3：使用递归
    
    Args:
        s (str): 输入字符串
        
    Returns:
        str: 反转后的字符串
    """
    # 基础情况：空字符串或单个字符
    if len(s) <= 1:
        return s
    
    # 递归：第一个字符放到最后，其余字符递归反转
    return reverse_string_recursive(s[1:]) + s[0]

# 测试代码
test_string = "Hello World"
print(f"原字符串: {test_string}")
print(f"方法1反转: {reverse_string_builtin(test_string)}")
print(f"方法2反转: {reverse_string_loop(test_string)}")
print(f"方法3反转: {reverse_string_recursive(test_string)}")
```

### Java实现

```java
public class StringReverser {
    /**
     * 方法1：使用StringBuilder
     * 
     * @param str 输入字符串
     * @return 反转后的字符串
     */
    public static String reverseStringBuilder(String str) {
        return new StringBuilder(str).reverse().toString();
    }
    
    /**
     * 方法2：使用循环
     * 
     * @param str 输入字符串
     * @return 反转后的字符串
     */
    public static String reverseStringLoop(String str) {
        StringBuilder result = new StringBuilder();
        for (int i = str.length() - 1; i >= 0; i--) {
            result.append(str.charAt(i));
        }
        return result.toString();
    }
    
    /**
     * 方法3：使用递归
     * 
     * @param str 输入字符串
     * @return 反转后的字符串
     */
    public static String reverseStringRecursive(String str) {
        // 基础情况：空字符串或单个字符
        if (str.length() <= 1) {
            return str;
        }
        
        // 递归：第一个字符放到最后，其余字符递归反转
        return reverseStringRecursive(str.substring(1)) + str.charAt(0);
    }
    
    // 测试代码
    public static void main(String[] args) {
        String testString = "Hello World";
        System.out.println("原字符串: " + testString);
        System.out.println("方法1反转: " + reverseStringBuilder(testString));
        System.out.println("方法2反转: " + reverseStringLoop(testString));
        System.out.println("方法3反转: " + reverseStringRecursive(testString));
    }
}
```

## 📚 本章小结

通过本章学习，你应该掌握：

1. **算法的基本概念**：理解什么是算法，算法的特征
2. **算法思维**：学会用算法的方式思考问题
3. **基本设计方法**：了解暴力法、贪心法等基本方法
4. **学习方法**：掌握学习算法的正确方法

---

## 🔄 补充：递归原理深入解析

### 什么是递归？

**递归**是一种解决问题的方法，其中函数调用自身来解决更小规模的相同问题。递归就像俄罗斯套娃，每一层都包含着更小的相同结构。

### 🎯 递归的三要素

1. **递归出口（基础情况）**：问题的最简单情况，不需要再递归
2. **递归体（递归情况）**：将大问题分解为小问题的逻辑
3. **收敛性**：确保递归能够向基础情况收敛

```javascript
/**
 * 递归计算阶乘 - 经典递归示例
 * n! = n × (n-1) × (n-2) × ... × 1
 * @param {number} n - 要计算阶乘的数
 * @returns {number} n的阶乘
 */
function factorial(n) {
    // 递归出口：0! = 1, 1! = 1
    if (n <= 1) {
        return 1;
    }
    
    // 递归体：n! = n × (n-1)!
    return n * factorial(n - 1);
}

// 执行过程可视化：
// factorial(5)
// = 5 × factorial(4)
// = 5 × 4 × factorial(3)
// = 5 × 4 × 3 × factorial(2)
// = 5 × 4 × 3 × 2 × factorial(1)
// = 5 × 4 × 3 × 2 × 1
// = 120

console.log(`5! = ${factorial(5)}`); // 输出: 120
```

#### Python实现

```python
def factorial(n):
    """
    递归计算阶乘 - 经典递归示例
    n! = n × (n-1) × (n-2) × ... × 1
    
    Args:
        n (int): 要计算阶乘的数
        
    Returns:
        int: n的阶乘
    """
    # 递归出口：0! = 1, 1! = 1
    if n <= 1:
        return 1
    
    # 递归体：n! = n × (n-1)!
    return n * factorial(n - 1)

# 测试阶乘计算
print(f"5! = {factorial(5)}")  # 输出: 120
```

#### Java实现

```java
public class Factorial {
    /**
     * 递归计算阶乘 - 经典递归示例
     * n! = n × (n-1) × (n-2) × ... × 1
     * 
     * @param n 要计算阶乘的数
     * @return n的阶乘
     */
    public static long factorial(int n) {
        // 递归出口：0! = 1, 1! = 1
        if (n <= 1) {
            return 1;
        }
        
        // 递归体：n! = n × (n-1)!
        return n * factorial(n - 1);
    }
    
    // 测试代码
    public static void main(String[] args) {
        System.out.println("5! = " + factorial(5)); // 输出: 120
    }
}
```

### 🌳 递归调用栈的工作原理

```javascript
/**
 * 递归求斐波那契数列（带调用栈追踪）
 * F(n) = F(n-1) + F(n-2), F(0) = 0, F(1) = 1
 */
function fibonacci(n, depth) {
    depth = depth || 0;
    // 打印调用栈深度，帮助理解递归过程
    const indent = '  '.repeat(depth);
    console.log(`${indent}计算 fibonacci(${n})`);
    
    // 基础情况
    if (n <= 1) {
        console.log(`${indent}返回 ${n}`);
        return n;
    }
    
    // 递归计算
    const result = fibonacci(n - 1, depth + 1) + fibonacci(n - 2, depth + 1);
    console.log(`${indent}fibonacci(${n}) = ${result}`);
    return result;
}

// 测试小数值，观察递归过程
console.log('=== 斐波那契递归过程 ===');
fibonacci(4);
```

#### Python实现

```python
def fibonacci(n, depth=0):
    """
    递归求斐波那契数列（带调用栈追踪）
    F(n) = F(n-1) + F(n-2), F(0) = 0, F(1) = 1
    
    Args:
        n (int): 要计算的斐波那契数列项
        depth (int): 递归深度，用于打印缩进
        
    Returns:
        int: 第n项斐波那契数
    """
    # 打印调用栈深度，帮助理解递归过程
    indent = '  ' * depth
    print(f"{indent}计算 fibonacci({n})")
    
    # 基础情况
    if n <= 1:
        print(f"{indent}返回 {n}")
        return n
    
    # 递归计算
    result = fibonacci(n - 1, depth + 1) + fibonacci(n - 2, depth + 1)
    print(f"{indent}fibonacci({n}) = {result}")
    return result

# 测试小数值，观察递归过程
print('=== 斐波那契递归过程 ===')
fibonacci(4)
```

#### Java实现

```java
public class Fibonacci {
    /**
     * 递归求斐波那契数列（带调用栈追踪）
     * F(n) = F(n-1) + F(n-2), F(0) = 0, F(1) = 1
     * 
     * @param n 要计算的斐波那契数列项
     * @param depth 递归深度，用于打印缩进
     * @return 第n项斐波那契数
     */
    public static int fibonacci(int n, int depth) {
        // 打印调用栈深度，帮助理解递归过程
        String indent = "  ".repeat(depth);
        System.out.println(indent + "计算 fibonacci(" + n + ")");
        
        // 基础情况
        if (n <= 1) {
            System.out.println(indent + "返回 " + n);
            return n;
        }
        
        // 递归计算
        int result = fibonacci(n - 1, depth + 1) + fibonacci(n - 2, depth + 1);
        System.out.println(indent + "fibonacci(" + n + ") = " + result);
        return result;
    }
    
    /**
     * 重载方法：使用默认深度0
     */
    public static int fibonacci(int n) {
        return fibonacci(n, 0);
    }
    
    // 测试小数值，观察递归过程
    public static void main(String[] args) {
        System.out.println("=== 斐波那契递归过程 ===");
        fibonacci(4);
    }
}
```

### ⚠️ 递归常见错误与解决方案

#### 错误1：缺少递归出口
```javascript
// ❌ 错误示例：无限递归
function badCountdown(n) {
    console.log(n);
    return badCountdown(n - 1); // 没有停止条件！
}

// ✅ 正确示例：有明确的递归出口
function goodCountdown(n) {
    // 递归出口
    if (n <= 0) {
        console.log('倒计时结束！');
        return;
    }
    
    console.log(n);
    goodCountdown(n - 1); // 向递归出口收敛
}

goodCountdown(5); // 输出: 5, 4, 3, 2, 1, 倒计时结束！
```

#### 错误2：递归效率问题
```javascript
// ❌ 低效的递归：重复计算
function inefficientFib(n) {
    if (n <= 1) return n;
    return inefficientFib(n - 1) + inefficientFib(n - 2);
    // 时间复杂度：O(2^n) - 非常慢！
}

// ✅ 优化方案1：记忆化递归
function memoizedFib(n, memo = {}) {
    // 如果已经计算过，直接返回结果
    if (n in memo) {
        return memo[n];
    }
    
    if (n <= 1) {
        return n;
    }
    
    // 存储计算结果，避免重复计算
    memo[n] = memoizedFib(n - 1, memo) + memoizedFib(n - 2, memo);
    return memo[n];
    // 时间复杂度：O(n) - 快很多！
}
```

#### Python实现

```python
def iterative_fib(n):
    """
    迭代实现斐波那契数列
    
    Args:
        n (int): 要计算的斐波那契数列项
        
    Returns:
        int: 第n项斐波那契数
    """
    if n <= 1:
        return n
    
    prev, curr = 0, 1
    for i in range(2, n + 1):
        prev, curr = curr, prev + curr
    return curr
    # 时间复杂度：O(n)，空间复杂度：O(1)
```

#### Java实现

```java
public class IterativeFibonacci {
    /**
     * 迭代实现斐波那契数列
     * 
     * @param n 要计算的斐波那契数列项
     * @return 第n项斐波那契数
     */
    public static long iterativeFib(int n) {
        if (n <= 1) {
            return n;
        }
        
        long prev = 0, curr = 1;
        for (int i = 2; i <= n; i++) {
            long temp = curr;
            curr = prev + curr;
            prev = temp;
        }
        return curr;
        // 时间复杂度：O(n)，空间复杂度：O(1)
    }
}
```

---

## 👉 指针操作基础（JavaScript中的引用）

虽然JavaScript没有传统意义上的指针，但理解引用的概念对算法学习至关重要，特别是在处理对象、数组和链表时。

### 🎯 值类型 vs 引用类型

```javascript
// 值类型（基本数据类型）：直接存储值
let a = 5;
let b = a;  // b得到a的副本
a = 10;     // 修改a不影响b
console.log(`a = ${a}, b = ${b}`); // 输出: a = 10, b = 5

// 引用类型（对象、数组）：存储引用地址
let arr1 = [1, 2, 3];
let arr2 = arr1;  // arr2指向同一个数组
arr1.push(4);     // 通过arr1修改数组
console.log('arr1:', arr1); // 输出: [1, 2, 3, 4]
console.log('arr2:', arr2); // 输出: [1, 2, 3, 4] - 也被修改了！
```



### 🔗 链表中的"指针"操作

```javascript
/**
 * 链表节点定义
 */
class ListNode {
    constructor(val = 0, next = null) {
        this.val = val;   // 节点值
        this.next = next; // 指向下一个节点的"指针"（引用）
    }
}

/**
 * 创建链表：1 -> 2 -> 3 -> null
 */
function createLinkedList() {
    const node3 = new ListNode(3, null);
    const node2 = new ListNode(2, node3);
    const node1 = new ListNode(1, node2);
    return node1; // 返回头节点
}

/**
 * 遍历链表（指针移动）
 */
function printLinkedList(head) {
    let current = head; // current是一个"指针"，指向当前节点
    const values = [];
    
    while (current !== null) {
        values.push(current.val);
        current = current.next; // 指针向前移动
    }
    
    console.log('链表内容:', values.join(' -> '));
}

// 测试链表操作
const list = createLinkedList();
printLinkedList(list); // 输出: 1 -> 2 -> 3
```

#### Python实现

```python
class ListNode:
    """
    链表节点定义
    """
    def __init__(self, val=0, next=None):
        self.val = val    # 节点值
        self.next = next  # 指向下一个节点的"指针"（引用）

def create_linked_list():
    """
    创建链表：1 -> 2 -> 3 -> None
    
    Returns:
        ListNode: 链表的头节点
    """
    node3 = ListNode(3, None)
    node2 = ListNode(2, node3)
    node1 = ListNode(1, node2)
    return node1  # 返回头节点

def print_linked_list(head):
    """
    遍历链表（指针移动）
    
    Args:
        head (ListNode): 链表的头节点
    """
    current = head  # current是一个"指针"，指向当前节点
    values = []
    
    while current is not None:
        values.append(str(current.val))
        current = current.next  # 指针向前移动
    
    print('链表内容:', ' -> '.join(values))

# 测试链表操作
list_head = create_linked_list()
print_linked_list(list_head)  # 输出: 1 -> 2 -> 3
```

#### Java实现

```java
/**
 * 链表节点定义
 */
class ListNode {
    int val;        // 节点值
    ListNode next;  // 指向下一个节点的"指针"（引用）
    
    // 构造函数
    ListNode() {}
    
    ListNode(int val) {
        this.val = val;
    }
    
    ListNode(int val, ListNode next) {
        this.val = val;
        this.next = next;
    }
}

public class LinkedListOperations {
    /**
     * 创建链表：1 -> 2 -> 3 -> null
     * 
     * @return 链表的头节点
     */
    public static ListNode createLinkedList() {
        ListNode node3 = new ListNode(3, null);
        ListNode node2 = new ListNode(2, node3);
        ListNode node1 = new ListNode(1, node2);
        return node1; // 返回头节点
    }
    
    /**
     * 遍历链表（指针移动）
     * 
     * @param head 链表的头节点
     */
    public static void printLinkedList(ListNode head) {
        ListNode current = head; // current是一个"指针"，指向当前节点
        StringBuilder values = new StringBuilder();
        
        while (current != null) {
            values.append(current.val);
            if (current.next != null) {
                values.append(" -> ");
            }
            current = current.next; // 指针向前移动
        }
        
        System.out.println("链表内容: " + values.toString());
    }
    
    // 测试链表操作
    public static void main(String[] args) {
        ListNode list = createLinkedList();
        printLinkedList(list); // 输出: 1 -> 2 -> 3
    }
}
```

### ⚠️ 指针操作常见错误

#### 错误1：空指针访问
```javascript
// ❌ 错误示例：没有检查null
function badTraversal(head) {
    let current = head;
    while (current.next !== null) { // 如果head为null，这里会报错！
        console.log(current.val);
        current = current.next;
    }
}

// ✅ 正确示例：先检查指针是否为null
function safeTraversal(head) {
    let current = head;
    while (current !== null) { // 先检查current是否为null
        console.log(current.val);
        current = current.next;
    }
}

// 测试空链表
safeTraversal(null); // 安全，不会报错
```

```python
# ❌ 错误示例：没有检查None
def bad_traversal(head):
    current = head
    while current.next is not None:  # 如果head为None，这里会报错！
        print(current.val)
        current = current.next

# ✅ 正确示例：先检查指针是否为None
def safe_traversal(head):
    current = head
    while current is not None:  # 先检查current是否为None
        print(current.val)
        current = current.next

# ❌ 错误示例：指针丢失
def bad_insert_at_head(head, val):
    new_node = ListNode(val)
    new_node.next = head
    head = new_node  # 这里只是修改了局部变量，原链表没变！
    return head

# ✅ 正确示例：返回新的头节点
def insert_at_head(head, val):
    new_node = ListNode(val)
    new_node.next = head
    return new_node  # 返回新的头节点

# 使用示例
my_list = create_linked_list()
print('原链表:')
print_linked_list(my_list)

my_list = insert_at_head(my_list, 0)  # 必须接收返回值！
print('插入后:')
print_linked_list(my_list)  # 输出: 0 -> 1 -> 2 -> 3

# 测试空链表
safe_traversal(None)  # 安全，不会报错
```

```java
// ❌ 错误示例：没有检查null
public static void badTraversal(ListNode head) {
    ListNode current = head;
    while (current.next != null) { // 如果head为null，这里会报错！
        System.out.println(current.val);
        current = current.next;
    }
}

// ✅ 正确示例：先检查指针是否为null
public static void safeTraversal(ListNode head) {
    ListNode current = head;
    while (current != null) { // 先检查current是否为null
        System.out.println(current.val);
        current = current.next;
    }
}

// ❌ 错误示例：指针丢失
public static ListNode badInsertAtHead(ListNode head, int val) {
    ListNode newNode = new ListNode(val);
    newNode.next = head;
    head = newNode; // 这里只是修改了局部变量，原链表没变！
    return head;
}

// ✅ 正确示例：返回新的头节点
public static ListNode insertAtHead(ListNode head, int val) {
    ListNode newNode = new ListNode(val);
    newNode.next = head;
    return newNode; // 返回新的头节点
}

// 使用示例
ListNode myList = createLinkedList();
System.out.println("原链表:");
printLinkedList(myList);

myList = insertAtHead(myList, 0); // 必须接收返回值！
System.out.println("插入后:");
printLinkedList(myList); // 输出: 0 -> 1 -> 2 -> 3

// 测试空链表
safeTraversal(null); // 安全，不会报错
```

#### 错误2：指针丢失
```javascript
/**
 * 在链表头部插入新节点
 */
// ❌ 错误示例：指针丢失
function badInsertAtHead(head, val) {
    const newNode = new ListNode(val);
    newNode.next = head;
    head = newNode; // 这里只是修改了局部变量，原链表没变！
    return head;
}

// ✅ 正确示例：返回新的头节点
function insertAtHead(head, val) {
    const newNode = new ListNode(val);
    newNode.next = head;
    return newNode; // 返回新的头节点
}

// 使用示例
let myList = createLinkedList();
console.log('原链表:');
printLinkedList(myList);

myList = insertAtHead(myList, 0); // 必须接收返回值！
console.log('插入后:');
printLinkedList(myList); // 输出: 0 -> 1 -> 2 -> 3
```

### 🎯 双指针技巧

```javascript
/**
 * 使用快慢指针找链表中点
 */
function findMiddle(head) {
    if (!head) return null;
    
    let slow = head;  // 慢指针，每次移动一步
    let fast = head;  // 快指针，每次移动两步
    
    // 当快指针到达末尾时，慢指针正好在中点
    while (fast && fast.next) {
        slow = slow.next;      // 慢指针移动一步
        fast = fast.next.next; // 快指针移动两步
    }
    
    return slow; // 返回中点节点
}


```

#### Python实现

```python
def find_middle(head):
    """
    使用快慢指针找链表中点
    
    Args:
        head (ListNode): 链表的头节点
        
    Returns:
        ListNode: 链表的中点节点，如果链表为空则返回None
    """
    if not head:
        return None
    
    slow = head  # 慢指针，每次移动一步
    fast = head  # 快指针，每次移动两步
    
    # 当快指针到达末尾时，慢指针正好在中点
    while fast and fast.next:
        slow = slow.next        # 慢指针移动一步
        fast = fast.next.next   # 快指针移动两步
    
    return slow  # 返回中点节点

# 测试双指针技巧
test_list = create_linked_list()
middle = find_middle(test_list)
print(f"链表中点的值: {middle.val}")  # 输出: 2
```

#### Java实现

```java
public class LinkedListTwoPointers {
    /**
     * 使用快慢指针找链表中点
     * 
     * @param head 链表的头节点
     * @return 链表的中点节点，如果链表为空则返回null
     */
    public static ListNode findMiddle(ListNode head) {
        if (head == null) {
            return null;
        }
        
        ListNode slow = head; // 慢指针，每次移动一步
        ListNode fast = head; // 快指针，每次移动两步
        
        // 当快指针到达末尾时，慢指针正好在中点
        while (fast != null && fast.next != null) {
            slow = slow.next;        // 慢指针移动一步
            fast = fast.next.next;   // 快指针移动两步
        }
        
        return slow; // 返回中点节点
    }
    
    // 测试双指针技巧
    public static void main(String[] args) {
        ListNode testList = createLinkedList();
        ListNode middle = findMiddle(testList);
        System.out.println("链表中点的值: " + middle.val); // 输出: 2
    }
}
```

---

## 🚨 算法调试与错误处理

### 🔍 调试技巧

```javascript
/**
 * 带调试信息的二分查找
 */
function debugBinarySearch(arr, target) {
    let left = 0;
    let right = arr.length - 1;
    let step = 0;
    
    console.log(`开始二分查找，目标值: ${target}`);
    console.log(`数组: [${arr.join(', ')}]`);
    
    while (left <= right) {
        step++;
        const mid = Math.floor((left + right) / 2);
        
        console.log(`\n第${step}步:`);
        console.log(`  搜索范围: [${left}, ${right}]`);
        console.log(`  中点索引: ${mid}, 中点值: ${arr[mid]}`);
        
        if (arr[mid] === target) {
            console.log(`  ✅ 找到目标值！`);
            return mid;
        } else if (arr[mid] < target) {
            console.log(`  中点值 < 目标值，搜索右半部分`);
            left = mid + 1;
        } else {
            console.log(`  中点值 > 目标值，搜索左半部分`);
            right = mid - 1;
        }
    }
    
    console.log(`\n❌ 未找到目标值`);
    return -1;
}
```

#### Python实现

```python
def debug_binary_search(arr, target):
    """
    带调试信息的二分查找
    
    Args:
        arr (list): 已排序的数组
        target (int): 要查找的目标值
        
    Returns:
        int: 目标值的索引，如果未找到返回-1
    """
    left = 0
    right = len(arr) - 1
    step = 0
    
    print(f"开始二分查找，目标值: {target}")
    print(f"数组: {arr}")
    
    while left <= right:
        step += 1
        mid = (left + right) // 2
        
        print(f"\n第{step}步:")
        print(f"  搜索范围: [{left}, {right}]")
        print(f"  中点索引: {mid}, 中点值: {arr[mid]}")
        
        if arr[mid] == target:
            print("  ✅ 找到目标值！")
            return mid
        elif arr[mid] < target:
            print("  中点值 < 目标值，搜索右半部分")
            left = mid + 1
        else:
            print("  中点值 > 目标值，搜索左半部分")
            right = mid - 1
    
    print("\n❌ 未找到目标值")
    return -1

# 测试调试版本
sorted_array = [1, 3, 5, 7, 9, 11, 13, 15]
debug_binary_search(sorted_array, 7)
```

#### Java实现

```java
public class DebugBinarySearch {
    /**
     * 带调试信息的二分查找
     * 
     * @param arr 已排序的数组
     * @param target 要查找的目标值
     * @return 目标值的索引，如果未找到返回-1
     */
    public static int debugBinarySearch(int[] arr, int target) {
        int left = 0;
        int right = arr.length - 1;
        int step = 0;
        
        System.out.println("开始二分查找，目标值: " + target);
        System.out.print("数组: [");
        for (int i = 0; i < arr.length; i++) {
            System.out.print(arr[i]);
            if (i < arr.length - 1) System.out.print(", ");
        }
        System.out.println("]");
        
        while (left <= right) {
            step++;
            int mid = left + (right - left) / 2;
            
            System.out.println("\n第" + step + "步:");
            System.out.println("  搜索范围: [" + left + ", " + right + "]");
            System.out.println("  中点索引: " + mid + ", 中点值: " + arr[mid]);
            
            if (arr[mid] == target) {
                System.out.println("  ✅ 找到目标值！");
                return mid;
            } else if (arr[mid] < target) {
                System.out.println("  中点值 < 目标值，搜索右半部分");
                left = mid + 1;
            } else {
                System.out.println("  中点值 > 目标值，搜索左半部分");
                right = mid - 1;
            }
        }
        
        System.out.println("\n❌ 未找到目标值");
        return -1;
    }
    
    // 测试调试版本
    public static void main(String[] args) {
        int[] sortedArray = {1, 3, 5, 7, 9, 11, 13, 15};
        debugBinarySearch(sortedArray, 7);
    }
}
```

### 🛡️ 错误处理最佳实践

```javascript
/**
 * 健壮的数组操作函数
 */
class SafeArrayOperations {
    /**
     * 安全的数组访问
     */
    static safeGet(arr, index, defaultValue = undefined) {
        // 参数验证
        if (!Array.isArray(arr)) {
            throw new Error('第一个参数必须是数组');
        }
        
        if (typeof index !== 'number' || !Number.isInteger(index)) {
            throw new Error('索引必须是整数');
        }
        
        // 边界检查
        if (index < 0 || index >= arr.length) {
            return defaultValue;
        }
        
        return arr[index];
    }
    
    /**
     * 安全的数组查找
     */
    static safeFind(arr, predicate) {
        try {
            if (!Array.isArray(arr)) {
                throw new Error('第一个参数必须是数组');
            }
            
            if (typeof predicate !== 'function') {
                throw new Error('第二个参数必须是函数');
            }
            
            for (let i = 0; i < arr.length; i++) {
                if (predicate(arr[i], i, arr)) {
                    return { found: true, value: arr[i], index: i };
                }
            }
            
            return { found: false, value: undefined, index: -1 };
            
        } catch (error) {
            console.error('查找过程中发生错误:', error.message);
            return { found: false, value: undefined, index: -1, error: error.message };
        }
    }
}

// 使用示例
const testArray = [1, 2, 3, 4, 5];

// 安全访问
console.log(SafeArrayOperations.safeGet(testArray, 2));    // 输出: 3
console.log(SafeArrayOperations.safeGet(testArray, 10));   // 输出: undefined
console.log(SafeArrayOperations.safeGet(testArray, 10, 'Not Found')); // 输出: 'Not Found'

// 安全查找
const result = SafeArrayOperations.safeFind(testArray, x => x > 3);
console.log(result); // 输出: { found: true, value: 4, index: 3 }
```

#### Python实现

```python
class SafeArrayOperations:
    """
    健壮的数组操作函数
    """
    
    @staticmethod
    def safe_get(arr, index, default_value=None):
        """
        安全的数组访问
        
        Args:
            arr (list): 输入数组
            index (int): 访问索引
            default_value: 默认返回值
            
        Returns:
            访问的元素或默认值
            
        Raises:
            TypeError: 参数类型错误时抛出
        """
        # 参数验证
        if not isinstance(arr, list):
            raise TypeError('第一个参数必须是列表')
        
        if not isinstance(index, int):
            raise TypeError('索引必须是整数')
        
        # 边界检查
        if index < 0 or index >= len(arr):
            return default_value
        
        return arr[index]
    
    @staticmethod
    def safe_find(arr, predicate):
        """
        安全的数组查找
        
        Args:
            arr (list): 输入数组
            predicate (function): 查找条件函数
            
        Returns:
            dict: 查找结果字典
        """
        try:
            if not isinstance(arr, list):
                raise TypeError('第一个参数必须是列表')
            
            if not callable(predicate):
                raise TypeError('第二个参数必须是函数')
            
            for i, item in enumerate(arr):
                if predicate(item):
                    return {'found': True, 'value': item, 'index': i}
            
            return {'found': False, 'value': None, 'index': -1}
            
        except Exception as error:
            print(f'查找过程中发生错误: {error}')
            return {'found': False, 'value': None, 'index': -1, 'error': str(error)}

# 使用示例
test_array = [1, 2, 3, 4, 5]

# 安全访问
print(SafeArrayOperations.safe_get(test_array, 2))    # 输出: 3
print(SafeArrayOperations.safe_get(test_array, 10))   # 输出: None
print(SafeArrayOperations.safe_get(test_array, 10, 'Not Found'))  # 输出: 'Not Found'

# 安全查找
result = SafeArrayOperations.safe_find(test_array, lambda x: x > 3)
print(result)  # 输出: {'found': True, 'value': 4, 'index': 3}
```

#### Java实现

```java
import java.util.HashMap;
import java.util.Map;
import java.util.function.Predicate;

/**
 * 健壮的数组操作函数
 */
public class SafeArrayOperations {
    
    /**
     * 安全的数组访问
     * 
     * @param arr 输入数组
     * @param index 访问索引
     * @param defaultValue 默认返回值
     * @return 访问的元素或默认值
     * @throws IllegalArgumentException 参数无效时抛出
     */
    public static <T> T safeGet(T[] arr, int index, T defaultValue) {
        // 参数验证
        if (arr == null) {
            throw new IllegalArgumentException("数组不能为null");
        }
        
        // 边界检查
        if (index < 0 || index >= arr.length) {
            return defaultValue;
        }
        
        return arr[index];
    }
    
    /**
     * 安全的数组查找
     * 
     * @param arr 输入数组
     * @param predicate 查找条件
     * @return 查找结果Map
     */
    public static <T> Map<String, Object> safeFind(T[] arr, Predicate<T> predicate) {
        Map<String, Object> result = new HashMap<>();
        
        try {
            if (arr == null) {
                throw new IllegalArgumentException("数组不能为null");
            }
            
            if (predicate == null) {
                throw new IllegalArgumentException("查找条件不能为null");
            }
            
            for (int i = 0; i < arr.length; i++) {
                if (predicate.test(arr[i])) {
                    result.put("found", true);
                    result.put("value", arr[i]);
                    result.put("index", i);
                    return result;
                }
            }
            
            result.put("found", false);
            result.put("value", null);
            result.put("index", -1);
            return result;
            
        } catch (Exception error) {
            System.err.println("查找过程中发生错误: " + error.getMessage());
            result.put("found", false);
            result.put("value", null);
            result.put("index", -1);
            result.put("error", error.getMessage());
            return result;
        }
    }
    
    // 使用示例
    public static void main(String[] args) {
        Integer[] testArray = {1, 2, 3, 4, 5};
        
        // 安全访问
        System.out.println(safeGet(testArray, 2, null));    // 输出: 3
        System.out.println(safeGet(testArray, 10, null));   // 输出: null
        System.out.println(safeGet(testArray, 10, -1));     // 输出: -1
        
        // 安全查找
        Map<String, Object> result = safeFind(testArray, x -> x > 3);
        System.out.println(result); // 输出: {found=true, value=4, index=3}
    }
}
```

---

## 📊 算法复杂度初步认识

### 🕐 时间复杂度直观理解

```javascript
/**
 * 不同时间复杂度的算法示例
 */
class ComplexityExamples {
    // O(1) - 常数时间：无论输入多大，执行时间固定
    static constant(arr) {
        console.log('O(1) 示例：访问数组第一个元素');
        return arr.length > 0 ? arr[0] : undefined;
    }
    
    // O(n) - 线性时间：执行时间与输入大小成正比
    static linear(arr) {
        console.log('O(n) 示例：遍历整个数组');
        let sum = 0;
        for (let i = 0; i < arr.length; i++) {
            sum += arr[i];
        }
        return sum;
    }
    
    // O(n²) - 平方时间：嵌套循环
    static quadratic(arr) {
        console.log('O(n²) 示例：冒泡排序');
        const result = [...arr]; // 复制数组
        
        for (let i = 0; i < result.length - 1; i++) {
            for (let j = 0; j < result.length - 1 - i; j++) {
                if (result[j] > result[j + 1]) {
                    // 交换元素
                    [result[j], result[j + 1]] = [result[j + 1], result[j]];
                }
            }
        }
        return result;
    }
    
    // O(log n) - 对数时间：二分查找
    static logarithmic(arr, target) {
        console.log('O(log n) 示例：二分查找');
        let left = 0, right = arr.length - 1;
        
        while (left <= right) {
            const mid = Math.floor(left + (right - left) / 2);
            if (arr[mid] === target) return mid;
            if (arr[mid] < target) left = mid + 1;
            else right = mid - 1;
        }
        return -1;
    }
}
```

#### Python实现

```python
import time

class ComplexityExamples:
    """
    不同时间复杂度的算法示例
    """
    
    @staticmethod
    def constant(arr):
        """
        O(1) - 常数时间：无论输入多大，执行时间固定
        
        Args:
            arr (list): 输入数组
            
        Returns:
            第一个元素或None
        """
        print('O(1) 示例：访问数组第一个元素')
        return arr[0] if len(arr) > 0 else None
    
    @staticmethod
    def linear(arr):
        """
        O(n) - 线性时间：执行时间与输入大小成正比
        
        Args:
            arr (list): 输入数组
            
        Returns:
            int: 数组元素的总和
        """
        print('O(n) 示例：遍历整个数组')
        total = 0
        for num in arr:
            total += num
        return total
    
    @staticmethod
    def quadratic(arr):
        """
        O(n²) - 平方时间：嵌套循环
        
        Args:
            arr (list): 输入数组
            
        Returns:
            list: 排序后的数组
        """
        print('O(n²) 示例：冒泡排序')
        result = arr.copy()  # 复制数组
        
        for i in range(len(result) - 1):
            for j in range(len(result) - 1 - i):
                if result[j] > result[j + 1]:
                    # 交换元素
                    result[j], result[j + 1] = result[j + 1], result[j]
        
        return result
    
    @staticmethod
    def logarithmic(arr, target):
        """
        O(log n) - 对数时间：二分查找
        
        Args:
            arr (list): 已排序的输入数组
            target: 要查找的目标值
            
        Returns:
            int: 目标值的索引，未找到返回-1
        """
        print('O(log n) 示例：二分查找')
        left, right = 0, len(arr) - 1
        
        while left <= right:
            mid = left + (right - left) // 2
            if arr[mid] == target:
                return mid
            elif arr[mid] < target:
                left = mid + 1
            else:
                right = mid - 1
        
        return -1

# 性能测试
def performance_test():
    """
    性能测试函数
    """
    sizes = [100, 1000, 10000]
    
    for size in sizes:
        print(f'\n=== 数组大小: {size} ===')
        test_array = list(range(1, size + 1))
        
        # 测试不同复杂度的算法
        start_time = time.time()
        ComplexityExamples.constant(test_array)
        print(f'O(1) 操作耗时: {(time.time() - start_time) * 1000:.4f}ms')
        
        start_time = time.time()
        ComplexityExamples.linear(test_array)
        print(f'O(n) 操作耗时: {(time.time() - start_time) * 1000:.4f}ms')
        
        start_time = time.time()
        ComplexityExamples.logarithmic(test_array, size // 2)
        print(f'O(log n) 操作耗时: {(time.time() - start_time) * 1000:.4f}ms')
        
        # O(n²) 操作只在小数组上测试，避免太慢
        if size <= 1000:
            start_time = time.time()
            ComplexityExamples.quadratic(test_array[:100])
            print(f'O(n²) 操作耗时: {(time.time() - start_time) * 1000:.4f}ms')

# 运行性能测试
# performance_test()
```

#### Java实现

```java
public class ComplexityExamples {
    /**
     * 不同时间复杂度的算法示例
     */
    
    /**
     * O(1) - 常数时间：无论输入多大，执行时间固定
     * 
     * @param arr 输入数组
     * @return 第一个元素或null
     */
    public static Integer constant(int[] arr) {
        System.out.println("O(1) 示例：访问数组第一个元素");
        return arr.length > 0 ? arr[0] : null;
    }
    
    /**
     * O(n) - 线性时间：执行时间与输入大小成正比
     * 
     * @param arr 输入数组
     * @return 数组元素的总和
     */
    public static long linear(int[] arr) {
        System.out.println("O(n) 示例：遍历整个数组");
        long sum = 0;
        for (int num : arr) {
            sum += num;
        }
        return sum;
    }
    
    /**
     * O(n²) - 平方时间：嵌套循环
     * 
     * @param arr 输入数组
     * @return 排序后的数组
     */
    public static int[] quadratic(int[] arr) {
        System.out.println("O(n²) 示例：冒泡排序");
        int[] result = arr.clone(); // 复制数组
        
        for (int i = 0; i < result.length - 1; i++) {
            for (int j = 0; j < result.length - 1 - i; j++) {
                if (result[j] > result[j + 1]) {
                    // 交换元素
                    int temp = result[j];
                    result[j] = result[j + 1];
                    result[j + 1] = temp;
                }
            }
        }
        return result;
    }
    
    /**
     * O(log n) - 对数时间：二分查找
     * 
     * @param arr 已排序的输入数组
     * @param target 要查找的目标值
     * @return 目标值的索引，未找到返回-1
     */
    public static int logarithmic(int[] arr, int target) {
        System.out.println("O(log n) 示例：二分查找");
        int left = 0, right = arr.length - 1;
        
        while (left <= right) {
            int mid = left + (right - left) / 2;
            if (arr[mid] == target) {
                return mid;
            } else if (arr[mid] < target) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }
        return -1;
    }
    
    /**
     * 性能测试
     */
    public static void performanceTest() {
        int[] sizes = {100, 1000, 10000};
        
        for (int size : sizes) {
            System.out.println("\n=== 数组大小: " + size + " ===");
            int[] testArray = new int[size];
            for (int i = 0; i < size; i++) {
                testArray[i] = i + 1;
            }
            
            // 测试不同复杂度的算法
            long startTime = System.nanoTime();
            constant(testArray);
            System.out.println("O(1) 操作耗时: " + 
                (System.nanoTime() - startTime) / 1_000_000.0 + "ms");
            
            startTime = System.nanoTime();
            linear(testArray);
            System.out.println("O(n) 操作耗时: " + 
                (System.nanoTime() - startTime) / 1_000_000.0 + "ms");
            
            startTime = System.nanoTime();
            logarithmic(testArray, size / 2);
            System.out.println("O(log n) 操作耗时: " + 
                (System.nanoTime() - startTime) / 1_000_000.0 + "ms");
            
            // O(n²) 操作只在小数组上测试，避免太慢
            if (size <= 1000) {
                int[] smallArray = new int[Math.min(100, size)];
                System.arraycopy(testArray, 0, smallArray, 0, smallArray.length);
                
                startTime = System.nanoTime();
                quadratic(smallArray);
                System.out.println("O(n²) 操作耗时: " + 
                    (System.nanoTime() - startTime) / 1_000_000.0 + "ms");
            }
        }
    }
    
    // 主函数用于测试
    public static void main(String[] args) {
        performanceTest();
    }
}

// 性能测试
function performanceTest() {
    const sizes = [100, 1000, 10000];
    
    sizes.forEach(size => {
        console.log(`\n=== 数组大小: ${size} ===`);
        const testArray = Array.from({length: size}, (_, i) => i + 1);
        
        // 测试不同复杂度的算法
        console.time('O(1) 操作');
        ComplexityExamples.constant(testArray);
        console.timeEnd('O(1) 操作');
        
        console.time('O(n) 操作');
        ComplexityExamples.linear(testArray);
        console.timeEnd('O(n) 操作');
        
        console.time('O(log n) 操作');
        ComplexityExamples.logarithmic(testArray, size / 2);
        console.timeEnd('O(log n) 操作');
        
        // O(n²) 操作只在小数组上测试，避免太慢
        if (size <= 1000) {
            console.time('O(n²) 操作');
            ComplexityExamples.quadratic(testArray.slice(0, 100));
            console.timeEnd('O(n²) 操作');
        }
    });
}

// 运行性能测试
performanceTest();
```

## 📚 本章小结

通过本章学习，你应该掌握：

### 🎯 核心概念
1. **算法的基本概念**：理解什么是算法，算法的特征
2. **递归原理**：掌握递归的三要素和常见错误
3. **指针操作**：理解JavaScript中的引用概念
4. **调试技巧**：学会调试算法和处理错误
5. **复杂度认识**：初步了解算法效率的衡量

### 🧠 算法思维
1. **分解问题**：将复杂问题分解为简单问题
2. **递归思维**：理解自相似的问题结构
3. **指针思维**：掌握引用和间接访问的概念
4. **调试思维**：系统性地发现和解决问题

### 🛠️ 实际技能
1. **代码实现**：能够实现基本的递归和迭代算法
2. **错误处理**：编写健壮的算法代码
3. **性能意识**：了解不同算法的效率差异
4. **调试能力**：快速定位和解决算法问题

## 🎯 下一步学习

接下来我们将学习**时间复杂度与空间复杂度**，这是评估算法效率的重要工具。掌握复杂度分析能帮你选择最优的算法解决方案。

---

**重要提示**：算法学习需要大量练习，建议你把每个代码示例都亲自运行一遍，并尝试修改参数观察结果的变化。特别是递归和指针操作，需要通过大量实践才能真正掌握！
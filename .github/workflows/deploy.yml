name: Deploy to GitHub Pages

on:
  push:
    branches: [main] # 或你使用的主分支名

jobs:
  deploy:
    runs-on: ubuntu-latest
    # 👇 关键：显式授予写权限
    permissions:
      contents: write  # 允许 push 到 gh-pages
      pages: write     # 如果用官方 Pages 部署也需要（可选）
      id-token: write  # 如果用 OIDC 也需要（这里不需要）
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci
        # 解释：如果存在 package-lock.json 用 npm ci（更快更可靠）
        # 如果没有，则 fallback 到 npm install

      - name: Build VitePress site
        run: npm run docs:build

      # 可选：调试输出目录
      - name: Debug build output
        run: ls -la ./.vitepress/dist

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./.vitepress/dist
